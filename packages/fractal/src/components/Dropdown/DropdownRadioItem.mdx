import {
  ArgTypes,
  Controls,
  Description,
  Meta,
  Primary,
  Subtitle,
  Title,
} from '@storybook/blocks'

import * as DropdownRadioItemStories from './DropdownRadioItem.stories'

<Meta of={DropdownRadioItemStories} />

<Title />

<Subtitle />

<Description />

---

## Props

On top of all the props, attributes and event handler you can set on a
`<InputRadio />` component (and thus a `<button />` element), the component
accepts the following props:

<ArgTypes />

## Playground

<Primary />

<Controls />
