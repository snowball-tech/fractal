import { Meta, Typeset } from '@storybook/blocks'

<Meta title="Atoms/Typography" />

# Typography

---

## Font family

Snowball's design took the side of using a close source and strongly marked font
called [PolySans](https://wearegradient.net/polysans/) created by
[Gradient](https://wearegradient.net/about/), an independant type foundry.

We use the `Standard` and `Wide` versions of this font in `400`, `600` and `800`
in both `normal` and `italic` styles.

> ##### ℹ️ Info
>
> ---
>
> When you download the open-source packages from Snowball's repository like
> [Fractal](https://github.com/snowball-tech/fractal) or
> [Glacier](https://github.com/snowball-tech/glacier), this font is
> _(obviously)_ not packaged.  
> If you want to use a close fallback, you can use:
>
> ```css
> .poly-sans-fallback {
>   font-family: Arial, system-ui, sans-serif;
>   letter-spacing: 0.2px;
> }
> .poly-sans-wide-fallback {
>   font-family: 'Arial Black', system-ui, sans-serif;
>   letter-spacing: 0.2px;
> }
> ```

For email content, the PolySans fallback "Arial" and "system-ui" are used.

> ##### ℹ️ Info
>
> ---
>
> When using the "Arial" or "Arial Black" fallback (either for emails or because
> you don't have access to the PolySans font), you should also use a `0.2px`
> letter spacing to improve readability.

```css
.email {
  font-family: Arial, system-ui, sans-serif;
  letter-spacing: 0.2px;
}
.email-wide {
  font-family: 'Arial Black', system-ui, sans-serif;
  letter-spacing: 0.2px;
}
```

## Levels

You can use 4 levels of typography in Fractal:

- **Display** are used for desktop and mobile marketing headings and punchlines.  
  It is subdivided in 2 levels, corresponding to each `<hX />` element.  
  There is also a separate `wide` variation.
- **Heading** are used for desktop and mobile product headings, titles and
  punchlines.  
  It is subdivided in 4 levels, corresponding to each `<hX />` element.  
  Levels 3 and 4 are also available in `link` variations
- **Body** are used for desktop and mobile standard paragraphs and contents.  
  It is subdivided in 2 levels _(regular and low)_.  
  Each is available in "normal", `median`, `bold` and `link` variations.
- **Caption** are used for desktop and mobile captions and small texts.
  It comes in 3 variations: `median`, `bold` and `link`.

## Usage

> ##### ℹ️ Info
>
> ---
>
> For a better showcase of the available typography, see the `<Typography />`
> component in the "Molecules" section.

### Display

**Font:** `PolySans, Arial, system-ui, sans-serif`  
**Weight:** `800`

#### Desktop

**Sizes:** `80px` (`display-1`), `72px` (`display-2`)

<Typeset
  fontFamily={`PolySans, Arial, system-ui, sans-serif`}
  fontSizes={[80, 72]}
  fontWeight={800}
  sampleText="May the force be with you!"
/>

#### Mobile

**Sizes:** `48px` (`display-1`), `40px` (`display-2`)

<Typeset
  fontFamily={`PolySans, Arial, system-ui, sans-serif`}
  fontSizes={[48, 40]}
  fontWeight={800}
  sampleText="May the force be with you!"
/>

#### Email and fallback

<Typeset
  fontFamily={`Arial, system-ui, sans-serif`}
  fontSizes={[80, 72, 48, 40]}
  fontWeight={800}
  data-fallback-letter-spacing
  sampleText="May the force be with you!"
/>

### Display Wide

**Font:** `PolySansWide, "Arial Black", system-ui, sans-serif`  
**Weight:** `800`

#### Desktop

**Size:** `56px`

<Typeset
  fontFamily={`PolySansWide, "Arial Black", system-ui, sans-serif`}
  fontSizes={[56]}
  fontWeight={800}
  sampleText="May the force be with you!"
/>

#### Mobile

**Size:** `32px`

<Typeset
  fontFamily={`PolySansWide, "Arial Black", system-ui, sans-serif`}
  fontSizes={[32]}
  fontWeight={800}
  sampleText="May the force be with you!"
/>

#### Email and fallback

<Typeset
  fontFamily={`"Arial Black", system-ui, sans-serif`}
  fontSizes={[56, 32]}
  fontWeight={800}
  data-fallback-letter-spacing
  sampleText="May the force be with you!"
/>

### Heading

**Font:** `PolySans, Arial, system-ui, sans-serif`  
**Weight:** `800` (regular and `-link`)

#### Desktop

**Sizes:** `40px` (`heading-1`), `32px` (`heading-2`), `28px` (`heading-3`), `24px` (`heading-4`)

<Typeset
  fontFamily={`PolySans, Arial, system-ui, sans-serif`}
  fontSizes={[40, 32, 24, 20]}
  fontWeight={800}
  sampleText="May the force be with you!"
/>

#### Mobile

**Sizes:** `32px` (`heading-1`), `28px` (`heading-2`), `24px` (`heading-3`), `20px` (`heading-4`)

<Typeset
  fontFamily={`PolySans, Arial, system-ui, sans-serif`}
  fontSizes={[32, 28, 24, 20]}
  fontWeight={800}
  sampleText="May the force be with you!"
/>

<Typeset
  fontFamily={`PolySans, Arial, system-ui, sans-serif`}
  fontSizes={[24, 20]}
  fontWeight={800}
  data-is-link
  sampleText="May the link be with you!"
/>

#### Email and fallback

<Typeset
  fontFamily={`Arial, system-ui, sans-serif`}
  fontSizes={[40, 32, 28, 24, 20]}
  fontWeight={800}
  data-fallback-letter-spacing
  sampleText="May the force be with you!"
/>

<Typeset
  fontFamily={`Arial, system-ui, sans-serif`}
  fontSizes={[24, 20]}
  fontWeight={800}
  data-is-link
  data-fallback-letter-spacing
  sampleText="May the link be with you!"
/>

### Body

**Font:** `PolySans, Arial, system-ui, sans-serif`  
**Sizes:** `16px` (`body-1`), `14px` (`body-2`)  
**Weights:** `400` (regular), `600` (`-median` and `-link`), `800` (`-bold`)

<Typeset
  fontFamily={`PolySans, Arial, system-ui, sans-serif`}
  fontSizes={[16, 14]}
  fontWeight={400}
  sampleText="May the force be with you!"
/>

<Typeset
  fontFamily={`PolySans, Arial, system-ui, sans-serif`}
  fontSizes={[16, 14]}
  fontWeight={600}
  sampleText="May the force be with you!"
/>

<Typeset
  fontFamily={`PolySans, Arial, system-ui, sans-serif`}
  fontSizes={[16, 14]}
  fontWeight={600}
  data-is-link
  sampleText="May the link be with you!"
/>

<Typeset
  fontFamily={`PolySans, Arial, system-ui, sans-serif`}
  fontSizes={[16, 14]}
  fontWeight={800}
  sampleText="May the force be with you!"
/>

#### Email and fallback

<Typeset
  fontFamily={`Arial, system-ui, sans-serif`}
  fontSizes={[16, 14]}
  fontWeight={400}
  data-fallback-letter-spacing
  sampleText="May the force be with you!"
/>

<Typeset
  fontFamily={`Arial, system-ui, sans-serif`}
  fontSizes={[16, 14]}
  fontWeight={600}
  data-fallback-letter-spacing
  sampleText="May the force be with you!"
/>

<Typeset
  fontFamily={`Arial, system-ui, sans-serif`}
  fontSizes={[16, 14]}
  fontWeight={600}
  data-is-link
  data-fallback-letter-spacing
  sampleText="May the link be with you!"
/>

<Typeset
  fontFamily={`Arial, system-ui, sans-serif`}
  fontSizes={[16, 14]}
  fontWeight={800}
  data-fallback-letter-spacing
  sampleText="May the force be with you!"
/>

### Caption

**Font:** `PolySans, Arial, system-ui, sans-serif`  
**Size:** `12px`  
**Weights:** `600` (`-median` and `-link`), `800` (`-bold`)

<Typeset
  fontFamily={`PolySans, Arial, system-ui, sans-serif`}
  fontSizes={[12]}
  fontWeight={600}
  sampleText="May the force be with you!"
/>

<Typeset
  fontFamily={`PolySans, Arial, system-ui, sans-serif`}
  fontSizes={[12]}
  fontWeight={600}
  data-is-link
  sampleText="May the link be with you!"
/>

<Typeset
  fontFamily={`PolySans, Arial, system-ui, sans-serif`}
  fontSizes={[12]}
  fontWeight={800}
  sampleText="May the force be with you!"
/>

#### Email and fallback

<Typeset
  fontFamily={`Arial, system-ui, sans-serif`}
  fontSizes={[12]}
  fontWeight={600}
  data-fallback-letter-spacing
  sampleText="May the force be with you!"
/>

<Typeset
  fontFamily={`Arial, system-ui, sans-serif`}
  fontSizes={[12]}
  fontWeight={600}
  data-is-link
  data-fallback-letter-spacing
  sampleText="May the link be with you!"
/>

<Typeset
  fontFamily={`Arial, system-ui, sans-serif`}
  fontSizes={[12]}
  fontWeight={800}
  data-fallback-letter-spacing
  sampleText="May the force be with you!"
/>
