import {
  ArgTypes,
  Controls,
  Description,
  Meta,
  Primary,
  Subtitle,
  Title,
} from '@storybook/blocks'

import * as AutocompleteStories from './Autocomplete.stories'

<Meta of={AutocompleteStories} />

<Title />

<Subtitle />

<Description />

---

## Props

On top of all the props, attributes and event handler you can set on a
`<Dropdown />` component (and thus a `<div />` element), the component accepts
the following props:

<ArgTypes />

## Playground

<Primary />

<Controls />
